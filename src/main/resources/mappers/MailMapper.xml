<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.activitiweb.mapper.MailMapper">

    <select id="getSubscribeUser" resultType="com.imooc.activitiweb.pojo.UserInfoBean">
        select * from user where is_subscribe=1;
    </select>

    <select id="getAllFormData" resultType="com.imooc.activitiweb.pojo.FormData">

    </select>
</mapper>