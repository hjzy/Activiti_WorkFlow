<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.activitiweb.mapper.ArticleMapper">
    <insert id="insertArticle" parameterType="com.imooc.activitiweb.pojo.Article">
        insert into article (title, author, content, type, date)
        values (#{title}, #{author}, #{content}, #{type}, NOW());
    </insert>

    <select id="getArticleById" resultType="com.imooc.activitiweb.pojo.Article" parameterType="int">
        select *
        from article
        where id = #{id};
    </select>

    <select id="getAllArticle" resultType="com.imooc.activitiweb.pojo.Article">
        select *
        from article
    </select>

    <select id="searchArticle" resultType="com.imooc.activitiweb.pojo.Article">
        select *
        from article
        where title LIKE '%${titleStr}%'
    </select>

    <delete id="deleteArticle" parameterType="int">
        delete
        from article
        where id = #{id};
    </delete>

    <update id="updateArticle">
        update article
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="author != null">
                author = #{author},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
        </set>
        where id = #{id};
    </update>

    <select id="getArticleByType" resultType="com.imooc.activitiweb.pojo.Article" parameterType="int">
        select *
        from article
        where type = #{type}
    </select>

    <select id="getArticleByTypeLimitSize" resultType="com.imooc.activitiweb.pojo.Article" parameterType="int">
        select *
        from article
        where type = #{type}
        limit #{size}
    </select>


    <select id="getArticleCountMapForEcharts" resultType="java.util.Map">
        select type, COUNT(type) as count
        from article
        group by type
    </select>
</mapper>